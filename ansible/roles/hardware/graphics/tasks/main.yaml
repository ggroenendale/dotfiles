- include_tasks: arch.yaml
  when: ansible_os_family == "Archlinux"

- include_tasks: debian.yaml
  when: ansible_os_family == "Debian"

- include_tasks: ../verify/main.yaml
  tags: verify

- set_fact:
    graphics_opengl_check_enabled: true
