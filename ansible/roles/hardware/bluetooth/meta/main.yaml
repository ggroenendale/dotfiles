---
galaxy_info:
  role_name: hardware/bluetooth
  description: Install Bluetooth utilities and configure special bluetooth devices (bluez, bluetoothctl)
  license: MIT
  min_ansible_version: "2.14"

  platforms:
    - name: ArchLinux
      versions:
        - all
    - name: Debian
      versions:
        - bookwork
        - bullseye
    - name: OpenSUSE
      versions:
        - all
    - name: OpenSUSE
      versions:
        - all
    - name: Ubuntu
      versions:
        - all

dependencies:
  # Requires nothing at the moment
  - role:
