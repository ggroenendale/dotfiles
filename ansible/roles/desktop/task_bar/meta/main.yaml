galaxy_info:
  role_name: desktop/task_bar
  description: Install task bar tools and plugins for the task bar (waybar)
  license: MIT
  min_ansible_version: "2.14"

  platforms:
    - name: ArchLinux
      versions:
        - all
    - name: Debian
      versions:
        - bookwork
        - bullseye
    - name: OpenSUSE
      versions:
        - all
    - name: OpenSUSE
      versions:
        - all
    - name: Ubuntu
      versions:
        - all

dependencies:
  # Requires cargo build
  #- role: common/rust
  # Requires wayand-protocol and wayland-client and cargo
  - role: desktop/wayland
